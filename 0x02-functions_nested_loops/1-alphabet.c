#include "main.h"
 /**
 *  main - check the code
 *  print_alphabet - decleration
 *
 * Return: Always 0.
 */
void print_alphabet(void)/**decleration found in main**/
{
char alpha;

for (alpha = 'a'; alpha <= 'z'; alpha++)
_putchar(alpha);
	_putchar('\n');
}
