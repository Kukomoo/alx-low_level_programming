#include "main.h"
/**
 * main - check the code
 * _islower - used to check if character is lowcase
 * int c is used to declare c
 *
 * Return: Always 0.
 */
int _islower(int c)
{
	int d = 1;
	int f = 0;

	if (c <= 'z' && c >= 'a')
	_putchar(d);
	else
{
	_putchar(f);
	}
}
